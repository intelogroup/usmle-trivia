# Test Environment Configuration
# Copy this file to .env.test and fill in your test values
# DO NOT commit .env.test to version control

# Convex Test Instance
VITE_CONVEX_URL=your_test_convex_url_here

# Test Configuration
VITE_APP_ENV=test
VITE_NODE_ENV=test

# Feature Flags for Testing
VITE_FEATURE_ANALYTICS=false
VITE_FEATURE_ERROR_REPORTING=false
VITE_FEATURE_PERFORMANCE_MONITORING=false

# Test User Credentials (use environment variables, never hardcode)
# These should be set in your CI/CD environment or local test setup
# TEST_USER_EMAIL=
# TEST_USER_PASSWORD=

# Note: For testing authentication, create test users programmatically
# within your test setup and teardown, rather than using hardcoded credentials